export default class Marker {
    id: string;
    lng: string;
    lat: string;

    constructor(id: string, lng: string, lat: string) {
        this.id = id;
        this.lng = lng;
        this.lat = lat;
    }
    
}